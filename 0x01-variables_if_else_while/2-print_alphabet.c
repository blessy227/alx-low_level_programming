#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 * main - main block
 * Description: Get a random number and check its last digit, compare it with 5
 * Return: 0
 */
int main(void)
{
	char c = 'a';

	char c;

	for (c = 'a'; c <= 'z'; c++)
	while (c <= 'z')
	{
		putchar(c);
		putchar('\n');
		c++;
	}

return (0);
	putchar('\n');
	return (0);
}
